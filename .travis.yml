os: linux
dist: focal
language: node_js
node_js: 14.15.1

services:
  - postgresql

before_script:
  - psql -c 'create database impactmarkettest;' -U postgres

install: yarn

script: yarn coverage:ci
